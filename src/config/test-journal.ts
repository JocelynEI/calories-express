/**
 * V3.1 — l'adresse du journal de test.
 *
 * Colle ici l'adresse que Google t'a donnée quand tu as publié le script
 * (voir docs/JOURNAL_DE_TEST.md). Elle ressemble à :
 *
 *     https://script.google.com/macros/s/AKfycb.../exec
 *
 * Tant que cette ligne reste vide, le journal n'existe pas : rien n'est
 * envoyé, et l'écran qui demande l'accord ne s'affiche pas. C'est aussi
 * comme ça qu'on arrête tout après le test — on vide la ligne, on republie.
 *
 * Cette adresse se retrouve dans le code public de ton dépôt. Ce n'est pas un
 * mot de passe : au pire, quelqu'un pourrait envoyer de fausses lignes dans
 * ton tableur. Pour un test entre proches c'est sans conséquence, et le jour
 * où tu arrêtes, tu supprimes le déploiement côté Google.
 */
export const TEST_JOURNAL_URL = '';
